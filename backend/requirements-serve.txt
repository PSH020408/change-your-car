# Runtime-only dependencies for the deployed API + HUD container.
# (training / ingest extras — fastf1, xgboost, shap, scipy, pytest — stay in requirements.txt)
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
orjson>=3.10
pandas>=2.2
numpy>=1.26
pyyaml>=6.0
lightgbm>=4.5
scikit-learn>=1.5
joblib>=1.4
